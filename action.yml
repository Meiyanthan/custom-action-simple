name: 'TimeZone'
description: 'Universal timezon with Your Name'

inputs:
  name-of-user:
    description: 'Your Name'
    required: true
    default: 'TimeZone'

outputs:
  time:
    description: 'The time of the message'

runs:
  using: 'node12'
  main: 'dist/index.js'